# Core Vector Operations & Indexing
hnswlib==0.8.0
numpy==1.26.4
sentence-transformers==3.0.1
scikit-learn==1.4.2
torch>=2.2.0

# API Communication
requests==2.32.2

# Data Handling
pandas==2.2.2

# Document Processing (Phase 0: Chunking)
docling-core==2.25.0
docling==2.28.4
transformers==4.42.0

# Audio Processing (Dynamically Split by Operating System)
mlx-whisper; sys_platform == 'darwin'   # Only installs on macOS (Apple Silicon)
openai-whisper; sys_platform == 'win32' # Only installs on Windows (NVIDIA GPU / CPU)
ffmpeg-python

# Build & Utilities
Cython
tqdm==4.66.2
PyYAML==6.0.1

# Web Server
fastapi==0.110.0
uvicorn[standard]==0.29.0
